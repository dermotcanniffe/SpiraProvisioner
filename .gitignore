.kiro/
.vscode/
.venv/
.env
.*.env
!.env.example

# Default structure file — ignored so client-specific configs aren't
# accidentally committed to the shared tool repo.
# Remove this line when working in a client-specific fork/repo.
spira-structure.json
