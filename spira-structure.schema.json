{
  "title": "Spira Project Structure",
  "description": "Defines the project structure to be provisioned in a Spira instance via the REST API. Pass this schema to an LLM to generate a valid structure file from a plain-English brief.",
  "type": "object",
  "required": ["program"],
  "additionalProperties": false,
  "properties": {
    "program": {
      "type": "object",
      "description": "The top-level Spira program (portfolio) that groups all products. The program must already exist in the Spira admin UI - it cannot be created via the API.",
      "required": ["name", "products"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "The exact name of an existing program in Spira.",
          "minLength": 1,
          "maxLength": 255,
          "examples": ["Oetker Portfolio", "ACME Test Program"]
        },
        "products": {
          "type": "array",
          "description": "One or more products (projects) to create within the program.",
          "minItems": 1,
          "items": { "$ref": "#/definitions/product" }
        }
      }
    }
  },
  "definitions": {
    "product": {
      "type": "object",
      "description": "A Spira product (project) to create within the program.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the product. Must be unique within the program.",
          "minLength": 1,
          "maxLength": 255,
          "examples": ["Oetker Boost phase A", "WMS System"]
        },
        "releases": {
          "type": "array",
          "description": "Releases (test periods / sprints) to create within this product.",
          "default": [],
          "items": { "$ref": "#/definitions/release" }
        },
        "customFields": {
          "type": "object",
          "description": "Custom list fields to add to test cases and test sets within this product.",
          "additionalProperties": false,
          "properties": {
            "testCases": {
              "type": "array",
              "description": "Custom list fields to add to test case artifacts.",
              "default": [],
              "items": { "$ref": "#/definitions/customField" }
            },
            "testSets": {
              "type": "array",
              "description": "Custom list fields to add to test set artifacts.",
              "default": [],
              "items": { "$ref": "#/definitions/customField" }
            }
          }
        },
        "testFolders": {
          "type": "array",
          "description": "Test case folders to create within this product, each optionally containing test cases.",
          "default": [],
          "items": { "$ref": "#/definitions/testFolder" }
        }
      }
    },
    "release": {
      "type": "object",
      "description": "A release or test period within a product. Created as Sprint type by default.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the release.",
          "minLength": 1,
          "maxLength": 255,
          "examples": ["Q3 2025 Testing", "Sprint 1", "Phase A - Release 1"]
        }
      }
    },
    "customField": {
      "type": "object",
      "description": "A custom list field to add to a Spira artifact type. Appears as a dropdown on the artifact form.",
      "required": ["name", "type", "values"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the custom field as it will appear in the Spira UI.",
          "minLength": 1,
          "maxLength": 255,
          "examples": ["Transaction Code", "Country", "Region", "Business Unit"]
        },
        "type": {
          "type": "string",
          "description": "The field type. Currently only list is supported.",
          "enum": ["list"]
        },
        "values": {
          "type": "array",
          "description": "The allowed dropdown values for the list field.",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 },
          "examples": [["VA01", "ZVA01"], ["DE", "IT", "FR"]]
        }
      }
    },
    "testFolder": {
      "type": "object",
      "description": "A folder for organising test cases within a product.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the test folder.",
          "minLength": 1,
          "maxLength": 255,
          "examples": ["Order Management", "Regression Suite", "Smoke Tests"]
        },
        "testCases": {
          "type": "array",
          "description": "Test cases to create inside this folder.",
          "default": [],
          "items": { "$ref": "#/definitions/testCase" }
        }
      }
    },
    "testCase": {
      "type": "object",
      "description": "A single test case to create within a test folder.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Display name for the test case. Should clearly describe what is being tested.",
          "minLength": 1,
          "maxLength": 255,
          "examples": ["Create Standard Sales Order", "Validate Mandatory Fields"]
        },
        "description": {
          "type": "string",
          "description": "A brief description of what the test case verifies and how.",
          "default": "",
          "examples": ["Verify that a standard sales order can be created successfully using transaction VA01."]
        }
      }
    }
  }
}
